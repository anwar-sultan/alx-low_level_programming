#include<stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char printer[] = "\"Programming is like building a multilingual puzzle";

	puts(printer);

	return (0);
}
